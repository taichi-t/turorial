'use strict'

// const sum = function(a,b,c){
//     return a + b + c;
// };

const sum = (a,b,c) =>{
    return a + b + c;
};//上の式とおなじ意味になる。

const total = sum(1,2,3) + sum(3,4,5);
console.log(total);  