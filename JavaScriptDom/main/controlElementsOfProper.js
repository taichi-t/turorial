'use strict';
{
const h1 = document.querySelector('h1');
 
// console.log(h1.title);
// h1.title = 'changed'

h1.style.color = 'gray';
h1.style.backgroundColor = 'pink';

//like the above, basically property name of HTML elements and name of JavaScript property relates.
//there are some of them is exception.
}