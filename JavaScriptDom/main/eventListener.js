'use strict'
{
    const button = document.querySelector('button');

    button.addEventListener('dblclick', ()=>{
        console.log('clicked');

        //you can srtch more addEventListener on 'mdn addeventlistener'
    })
}